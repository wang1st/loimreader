# LoimReader 快速参考

## 一页纸速查

### 发布新版本（2.7.1）

```bash
# 最快速的方式（一条命令完成所有）
./release.sh 2.7.1 -u

# 分步操作
./release.sh 2.7.1              # 更新版本、构建
cd ../deploy/uploads/loimreader  # 进入上传目录
./upload_to_oss.sh              # 上传到OSS
```

### 日常开发

```bash
# 快速构建测试
./onekey_mac_deploy.sh

# 清理后重新构建
./onekey_mac_deploy.sh -c

# 运行验证脚本
./verify_setup.sh
```

### 常用文件位置

| 文件 | 位置 |
|------|------|
| 输出DMG | `../deploy/uploads/loimreader/LoimReader_v2.7.2_macOS.dmg` |
| version.json | `../deploy/uploads/loimreader/version.json` |
| 上传脚本 | `../deploy/uploads/loimreader/upload_to_oss.sh` |
| OSS密钥 | `../deploy/oss-config.sh` |

### 验证链接

```bash
# version.json
curl -s https://limereader-releases.oss-cn-hangzhou.aliyuncs.com/updates/loimreader/version.json | python3 -m json.tool

# DMG下载链接
https://limereader-releases.oss-cn-hangzhou.aliyuncs.com/updates/loimreader/LoimReader_v2.7.2_macOS.dmg
```

### 版本号位置

修改这三个文件：

1. `CMakeLists.txt` 第5行：`VERSION 2.7.1`
2. `app_version.h` 第42行：`return "2.7.1";`
3. `onekey_mac_deploy.sh` 第17行：`PROJECT_VERSION="2.7.1"`

### 关键命令

| 命令 | 用途 |
|------|------|
| `./release.sh 2.7.1` | 发布新版本 |
| `./onekey_mac_deploy.sh -c` | 清理并构建 |
| `./verify_setup.sh` | 验证环境 |
| `./install_ossutil.sh` | 安装ossutil |
| `ossutil version` | 查看ossutil版本 |
| `git tag` | 查看所有版本标签 |

### 故障排查

| 问题 | 解决方法 |
|------|---------|
| 找不到 Qt | `export PATH="/opt/homebrew/opt/qt@6/bin:$PATH"` |
| 找不到 ossutil | `./install_ossutil.sh` |
| 版本号不一致 | `./release.sh <版本号>` 自动同步 |
| DMG损坏 | `xattr -cr *.app && codesign --force --deep --sign - *.app` |

### 文档索引

| 文档 | 用途 |
|------|------|
| [一键部署指南.md](./一键部署指南.md) | ⭐ 主要使用文档 |
| [发布2.7.1示例.md](./发布2.7.1示例.md) | 完整发布示例 |
| [VERSION_MANAGEMENT.md](./VERSION_MANAGEMENT.md) | 版本管理详解 |
| [OSSUTIL_INSTALL.md](./OSSUTIL_INSTALL.md) | ossutil安装 |
| [OSS_SETUP.md](./OSS_SETUP.md) | OSS配置 |
| [部署完成摘要.md](./部署完成摘要.md) | 系统总览 |

### 环境变量

```bash
# Qt路径
export PATH="/opt/homebrew/opt/qt@6/bin:$PATH"

# OSS密钥（自动加载）
source ../deploy/oss-config.sh
```

### Git工作流

```bash
# 查看当前版本
git describe --tags

# 查看所有版本
git tag | grep "v2"

# 切换到特定版本
git checkout v2.7.1

# 回到最新
git checkout main
```

---

**打印此页并贴在显示器旁** 📌

