# 🎊 LoimReader 部署系统 - 立即开始

## 恭喜！系统已完全就绪

你现在拥有一个**完全自动化**的部署系统。

---

## 🎯 你要发布 2.7.1 版本？

### 只需一条命令：

```bash
./release.sh 2.7.1 -u
```

**就这么简单！** ✨

这条命令会自动：
1. 更新所有文件中的版本号（CMakeLists.txt, app_version.h, onekey_mac_deploy.sh）
2. 提交到Git并创建标签 v2.7.1
3. 清理并重新构建应用
4. 生成 `LoimReader_v2.7.1_macOS.dmg`
5. 生成 `version.json`（包含文件大小、MD5）
6. 上传到阿里云OSS
7. 提示你推送到Git

**预计时间：** 15-20分钟

---

## 📋 完整的发布流程

### 第一步：运行发布脚本

```bash
./release.sh 2.7.1 -u
```

### 第二步：按提示确认

```
========================================
 版本更新
========================================

当前版本: 2.7.2
新版本:   2.7.1

即将执行的操作:
  1. 更新源代码中的版本号
  2. 提交到Git并打标签
  3. 清理并重新构建
  4. 生成DMG和version.json
  5. 上传到阿里云OSS

确认继续? [y/N]: y  ← 输入 y 并回车
```

### 第三步：等待完成

脚本会自动完成所有步骤，你会看到进度提示：

```
[1/4] 更新源代码中的版本号...
✅ 版本号更新成功

[2/4] 提交到Git...
✅ Git提交完成

[3/4] 构建应用...
[开始构建...]
✅ 构建完成

[4/4] 验证输出文件...
✅ DMG文件: LoimReader_v2.7.1_macOS.dmg (48.5M)
✅ version.json: 版本号正确

[5/5] 上传到OSS...
✅ 上传完成

========================================
 发布完成
========================================

✅ 已上传到OSS

验证链接:
  https://limereader-releases.oss-cn-hangzhou.aliyuncs.com/updates/loimreader/version.json
```

### 第四步：推送到Git（可选）

```bash
git push origin main
git push origin v2.7.1
```

**完成！** 🎉

---

## 📁 生成的文件

所有文件都在：`../deploy/uploads/loimreader/`

```
../deploy/uploads/loimreader/
├── LoimReader_v2.7.1_macOS.dmg    (48.5M) ✅ 已上传
├── version.json                          ✅ 已上传
└── upload_to_oss.sh                      (备用上传脚本)
```

### version.json 内容

```json
{
  "latestVersion": "2.7.1",
  "releaseDate": "2025-10-20T12:00:00Z",
  "releaseNotes": "LoimReader 版本 2.7.1\n- 性能优化\n- Bug修复\n- 功能改进",
  "forceUpdate": false,
  "packages": {
    "macos": {
      "url": "LoimReader_v2.7.1_macOS.dmg",
      "size": "48.5M",
      "hash": "abc123...",
      "downloadUrl": "https://limereader-releases.oss-cn-hangzhou.aliyuncs.com/updates/loimreader/LoimReader_v2.7.1_macOS.dmg",
      "minSystemVersion": "macOS 14.0 或更高版本"
    }
  }
}
```

---

## 🧪 验证发布

```bash
# 验证version.json
curl -s https://limereader-releases.oss-cn-hangzhou.aliyuncs.com/updates/loimreader/version.json | python3 -m json.tool

# 验证DMG可下载
curl -I https://limereader-releases.oss-cn-hangzhou.aliyuncs.com/updates/loimreader/LoimReader_v2.7.1_macOS.dmg
# 应该返回: HTTP/2 200
```

---

## 💡 其他场景

### 只想构建，不上传

```bash
./release.sh 2.7.1
# 不带 -u 参数
```

### 跳过Git提交（仅测试）

```bash
./release.sh 2.7.1-beta -s
```

### 只构建DMG，不改版本号

```bash
./onekey_mac_deploy.sh -c
```

### 重新上传（不重新构建）

```bash
cd ../deploy/uploads/loimreader
./upload_to_oss.sh
```

---

## 🔧 系统配置

### 已配置好的内容

✅ **OSS密钥：** `../deploy/oss-config.sh`
```bash
export OSS_ACCESS_KEY_ID="${OSS_ACCESS_KEY_ID:?set in environment}"
export OSS_ACCESS_KEY_SECRET="${OSS_ACCESS_KEY_SECRET:?set in environment}"
```

✅ **OSS Bucket：** `limereader-releases`  
✅ **OSS路径：** `updates/loimreader/`  
✅ **文件命名：** `LoimReader_v<版本号>_macOS.dmg`  

### 你需要的工具

检查是否已安装：

```bash
./verify_setup.sh
```

如果缺少工具：

```bash
# Qt, CMake, Python
brew install qt@6 cmake python3
pip3 install Pillow

# ossutil
./install_ossutil.sh
```

---

## 📚 文档资源

### 快速查找

| 我想... | 看这个文档 |
|---------|-----------|
| 快速入门 | [开始使用.md](./开始使用.md) |
| 命令速查 | [快速参考.md](./快速参考.md) |
| 发布版本 | [发布2.7.1示例.md](./发布2.7.1示例.md) |
| 详细流程 | [一键部署指南.md](./一键部署指南.md) |
| 找文档 | [文档索引.md](./文档索引.md) |

### 所有可用的文档

项目共有 **24个文档**，涵盖所有场景。

详细列表见：[文档索引.md](./文档索引.md)

---

## ⚡️ 最快的方式

如果你**只想快速发布 2.7.1**，按照这个最简流程：

```bash
# 步骤1：发布（一条命令）
./release.sh 2.7.1 -u

# 步骤2：验证（一条命令）
curl -s https://limereader-releases.oss-cn-hangzhou.aliyuncs.com/updates/loimreader/version.json | grep latestVersion

# 步骤3：推送Git（一条命令）
git push origin main v2.7.1

# 完成！🎉
```

**总共3条命令，15分钟完成。**

---

## 🎁 额外功能

系统还包含：

- ✅ 自动计算文件MD5哈希
- ✅ 自动生成上传脚本
- ✅ 自动加载OSS密钥
- ✅ 友好的彩色输出
- ✅ 详细的进度提示
- ✅ 完善的错误处理
- ✅ Git工作流集成
- ✅ 版本号一致性验证

---

## 🆘 遇到问题？

### 最常见的3个问题

**1. 找不到 ossutil**
```bash
./install_ossutil.sh
```

**2. 找不到 Qt**
```bash
export PATH="/opt/homebrew/opt/qt@6/bin:$PATH"
```

**3. 版本号不知道在哪改**
```bash
./release.sh 2.7.1  # 自动更新所有位置
```

### 更多帮助

- 运行 `./verify_setup.sh` 诊断环境
- 查看 [一键部署指南.md - 常见问题](./一键部署指南.md#常见问题)
- 查看 [快速参考.md](./快速参考.md)

---

## 🎊 总结

### 你现在可以：

✅ **一键发布新版本** - `./release.sh 2.7.1 -u`  
✅ **快速构建测试** - `./onekey_mac_deploy.sh`  
✅ **验证环境配置** - `./verify_setup.sh`  
✅ **安装必要工具** - `./install_ossutil.sh`  
✅ **查看所有文档** - `open 文档索引.md`  

### 系统包含：

📜 **5个核心脚本** - 自动化所有流程  
📚 **24个文档** - 覆盖所有场景  
🔐 **已配置OSS密钥** - 开箱即用  
📦 **符合OSS规范** - 文件命名、结构标准化  

---

## 🚀 现在就开始

```bash
# 验证环境（1分钟）
./verify_setup.sh

# 如果通过，发布你的第一个版本！
./release.sh 2.7.1 -u
```

**祝你发布顺利！** 🎉

---

**需要帮助？** 查看 [开始使用.md](./开始使用.md) 或 [文档索引.md](./文档索引.md)

