## 项目概览

- **项目名称**: `ctdy123`（窗口标题“影谷长图打印”）
- **用途**: 长图或 PDF 首页自动分页、预览、打印，或导出 A4 PDF；支持自动/手动分页、边距调节、单双列排版、页码显示。
- **技术栈**: Qt Widgets（Core/Gui/Widgets/PrintSupport/Network/XML）、Poppler-Qt5（PDF 渲染）、Fervor（自动更新）、自定义弹窗组件。
- **国际化**: 简体中文/英文（`i18n/ctdy123_*.ts` + 运行时 `QTranslator`）。默认中文，可在 `user.ini` 设 `language=en`。
- **版本与更新**: 版本定义于 `clps.pro` 的 `VERSION`（当前 2.5），通过 Fervor Appcast RSS 检查更新。

## 运行流程

- **`main.cpp`**
  - 创建 `QApplication`（Qt6 环境注意 `QTextCodec` 迁移）。
  - 从 `user.ini` 读取语言与 `autopage` 并应用。
  - 根据语言配置 Fervor 更新源（`ctdy123.com`/`limereader.com`），静默检查更新。
  - 创建并显示 `MainWindow`。

- **`MainWindow`**
  - 布局：左 `LeftView`（缩略/预览），右 `MainView`（主画布），`QSplitter` 分栏。
  - 菜单/工具栏：打开、保存（PDF）、打印、双列切换、页码显示开关、边距缩放、画布缩放、帮助、购买。
  - 文件加载 `loadFile`：
    - 支持 `.pdf`、`.jpg`、`.png`、`.jpeg`（PDF 仅渲染第一页）。
    - PDF 采用 Poppler 渲染，若含文本尝试多级 DPI 提升清晰度。
    - 将长图切分为多个 `PageItem`（A4 比例约 0.7：`height = width / 0.7`）。
    - 生成 `PreviewItem` 单页缩略；每 4 页聚合 2×2 预览，可双场景切换。
    - 从 `user.ini` 读取 `marginsc` 初始化页边距；启用 `m_autopage` 时执行 `autoCut()`。
    - 根据是否生成内容，启用保存/打印/缩放等动作。
  - 导出与打印：
    - 保存：通过 `QPrinter` 设 A4 纵向、PDF 输出文件，调用 `print` 逐页渲染 `PreviewItem`。
    - 打印：弹出系统打印对话框输出到设备。
    - `print`：按视窗比例逐页打印，必要时 `newPage()`。
  - 交互：
    - 左右视图 `fitViewPort` 自适应；缩放按钮仅影响 `MainView`。
    - 支持拖放打开（Win/Linux/Mac 路径前缀兼容）。
    - 双列：`m_leftScene`（单列）与 `m_leftScene2`（双列）切换。
    - 页码：切换 `PreviewItem::g_printPageNum` 后刷新所有预览。
    - 边距：遍历 `PreviewItem` 调整并写回 `user.ini`。
  - 许可与水印：
    - 启动时 `loadLicense` 远程查询，更新窗口“已注册/未注册”。
    - 通过 `machineIDHash()` 计算 32 位 `uuidHash`，若为 997549406，则预览页绘制对角水印（随语言）。

- **自动更新（Fervor）**
  - 下载并解析 Appcast，匹配平台与版本；显示更新窗口/确认框；确认后用浏览器打开下载链接。
  - 依赖 `QNetworkAccessManager`、`QXmlStreamReader`，支持忽略旧版本（`fvignoredversions`）。

## 预览与分页

- `PageItem`：主图切片。支持分割线拖拽（`SplitVCursor`），在上一/下一页间分配高度并维护全局打印比 `g_ratio`；`autoCut()` 在尾部 30% 窗口以阈值 192 搜索最大“白”行定位切割。
- `PreviewItem`：打印页预览（单页或 2×2 聚合），支持页码叠绘、选中高亮、双击切换页码、点击聚焦主视图；边距按比例（默认 10%，0–30%）。
- `MainView`/`LeftView`：`QGraphicsView` 子类，`resizeEvent` 按宽度自适应；`MainView` 保留用户缩放 `m_scale`（默认 0.75）。

## 配置与工具

- `Util`：使用 `QSettings` 读写 `user.ini` 的 `config/<key>`。
- `PopUp`：透明圆角、淡入淡出提示条。
- `BuyDialog`：输入卡密验证/绑定；商店图标/链接随语言；成功后提示重启解锁。
- `HelpDialog`：帮助窗口，语言切换 Logo。
- `machineid`：跨平台机器指纹（Windows 注册表 MachineGuid、macOS `ioreg`、Linux `machine-id`），并提供 MD5 Hash 与分组显示。

## 构建与依赖

- `clps.pro`（主工程）
  - Qt 模块：core、gui、widgets、printsupport、network、xml
  - 资源：`clps.qrc`（图标、图片、翻译、UI 表单）
  - `DEFINES` 暴露 `FA_APP_VERSION` / `FA_APP_NAME`
  - 链接 Poppler-Qt5（Windows 提供 `lib/` 与 `include/`）
  - 集成 Fervor：`!include("fervor/Fervor.pri")`
  - `TRANSLATIONS` 指向 `.ts`；Windows `RC_ICONS` 指定 `icons/main.ico`
- `CMakeLists.txt`：简单桥接，声明 Fervor 子工程与链接关系。

## 关键设计点与模式

- 图像分页模型：`PageItem` 双向链表与 `PreviewItem` 的多对一/一对多；`onAdjusted()` 刷新预览；用 `g_ratio` 统一打印比例。
- 组件职责分离：主窗口协调；视图负责缩放和展示；图元负责自绘与交互。
- 外部状态持久化：`user.ini` 保存语言、目录、导出路径、边距比例、自动分页开关。

## 兼容性与注意事项

- Qt5/Qt6 差异：
  - `QTextCodec` 在 Qt6 需迁移（已通过版本宏处理）。
  - Poppler 绑定为 Qt5，迁移 Qt6 可改用 `poppler-qt6` 或 Qt PDF。
- PDF 渲染：当前仅取第一页；可扩展为多页拼接/逐页加载。
- 自动裁切：阈值固定 192，适合白底图；极端场景可能不理想。
- 水印与注册：未注册判定与服务端耦合，建议由服务端下发授权状态。
- 国际化：已提供中/英；动态切换需注意 UI 刷新时机。
- 路径：三平台 `file:///` 兼容，`QFileDialog` 返回风格兼容。

## 性能与改进建议

- 稳定性/鲁棒性：为网络层增加超时、重试、错误分类；隐藏/替换硬编码密钥；严格校验 JSON。
- 文件加载：支持 PDF 多页；超大图像分块/异步渲染避免阻塞 UI。
- 预览/打印：在高 DPI 下校准边距与比例；用 `QPageSize/QPageLayout` 与 `devicePixelRatio`。
- 性能：自动裁切/渲染放后台线程（信号/槽回传）。
- 可维护性：参数集中配置；为核心类补充文档；抽象“分页策略”。
- 体验：撤销/重做；预估页数；导出模板；“最近文件”与“语言切换”。
- 跨平台/构建：全面迁移 Qt6、升级 Poppler 或改 Qt PDF；完善打包脚本；引入 CI。

## 关键文件一览

- `main.cpp`、`mainwindow.{h,cpp}`、`mainview.{h,cpp}`、`leftview.{h,cpp}`
- `pageitem.{h,cpp}`、`previewitem.{h,cpp}`、`util.{h,cpp}`、`popup.{h,cpp}`
- `buydialog.*`、`helpdialog.*`、`fervor/*`
- `clps.pro`、`CMakeLists.txt`、`lib/`、`include/`、`icons/`、`images/`、`i18n/`、`*.ui`、`clps.qrc`

## 运行要点（Windows）

- 需要 Qt 运行时；可用 `run_qt6_app.cmd` 调用 `windeployqt` 自动复制 DLL。
- 运行目录需包含 `ctdy123_en.qm`、`icons/`、`images/`、`user.ini`（首次可自动创建）。
- 若使用 Qt5+Poppler：确保 `libpoppler-qt5.dll` 等在 PATH 或位于 exe 同目录。