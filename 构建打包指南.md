# 构建打包指南

本指南涵盖 Windows 和 macOS 平台的构建、打包和测试流程。

> **重要提示：** 本项目已采用统一的一键部署脚本，请优先参考 [一键部署指南.md](./一键部署指南.md)

## 快速参考

### macOS 一键部署

```bash
# 完整构建、打包和生成version.json（推荐）
./onekey_mac_deploy.sh

# 清理后重新构建
./onekey_mac_deploy.sh -c

# 构建并上传到OSS
./onekey_mac_deploy.sh -u
```

### Windows 一键部署

```cmd
# 双击运行或命令行执行
onekey_win_deploy.bat
```

### 前置要求

**macOS:**
- Xcode Command Line Tools
- Qt 6
- CMake 3.25+
- Python 3 + Pillow
- ossutil（可选，用于上传）

**Windows:**
- Qt 6.8.1 MinGW
- Python 3
- ossutil（可选，用于上传）

---

## macOS 平台

### 快速开始：一键部署

项目提供了 `onekey_mac_deploy.sh` 脚本，可以一键完成构建、打包DMG和生成version.json。

#### 基本用法

```bash
# 1. 完整构建和打包（推荐）
./onekey_mac_deploy.sh

# 2. 清理后重新构建
./onekey_mac_deploy.sh -c

# 3. 指定版本号
./onekey_mac_deploy.sh -v 2.7.3

# 4. 构建并上传到OSS
./onekey_mac_deploy.sh -u

# 5. 查看帮助
./onekey_mac_deploy.sh -h
```

### 环境准备

#### 必需工具

1. **Xcode Command Line Tools**
   ```bash
   xcode-select --install
   ```

2. **Homebrew**
   ```bash
   /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
   ```

3. **Qt 6**
   ```bash
   brew install qt@6
   ```

4. **CMake**
   ```bash
   brew install cmake
   ```

5. **Python 3 (用于生成图标和背景图)**
   ```bash
   brew install python3
   pip3 install Pillow
   ```

#### 验证环境

```bash
# 检查工具版本
cmake --version        # 应显示 3.25 或更高
qmake --version        # 应显示 Qt 6.x
python3 --version      # 应显示 Python 3.x
```

### 详细构建流程

#### 方法一：使用一键脚本（推荐）

```bash
# 完整构建流程
./build_dmg.sh -b -c

# 脚本会自动完成：
# 1. 清理旧的构建文件
# 2. 生成应用图标和DMG背景图
# 3. 配置CMake
# 4. 编译应用程序
# 5. 部署Qt依赖库
# 6. 修复应用签名
# 7. 创建DMG安装包
```

#### 方法二：使用综合构建脚本

```bash
# 使用 build.sh（功能更丰富）
./build.sh all          # 完整构建流程
./build.sh build        # 仅构建应用
./build.sh dmg          # 仅创建DMG
./build.sh clean        # 清理构建文件
```

#### 方法三：手动步骤

如果需要更精细的控制，可以手动执行：

```bash
# 1. 清理旧文件
rm -rf build_cmake ../deploy/uploads/loimreader

# 2. 生成资源
python3 create_app_icon.py
python3 create_dmg_background.py

# 3. 创建构建目录
mkdir -p build_cmake
cd build_cmake

# 4. 配置CMake
cmake -DCMAKE_BUILD_TYPE=Release ..

# 5. 编译
make -j$(sysctl -n hw.ncpu)

# 6. 部署Qt依赖
cd bin
macdeployqt LoimReader.app -verbose=2

# 7. 修复签名
cd ../..
xattr -cr build_cmake/bin/LoimReader.app
codesign --force --deep --sign - build_cmake/bin/LoimReader.app

# 8. 打包和生成version.json（使用脚本）
./onekey_mac_deploy.sh -s  # -s 跳过构建，仅打包
```

### DMG 打包详解

#### DMG 内容结构

```
LoimReader_v2.7.2_macOS.dmg
├── LoimReader.app
├── Applications (符号链接)
├── 使用说明.txt
└── .background/
    └── background.png
```

#### DMG 文件命名规范

符合阿里云OSS发布规范：

```
格式: <ClientName>_v<Version>_<Platform>.<ext>
示例: LoimReader_v2.7.2_macOS.dmg
```

#### 自定义DMG

编辑 `onekey_mac_deploy.sh` 修改以下参数：

```bash
# 客户端名称
CLIENT_NAME="LoimReader"

# 版本号（或使用命令行参数 -v）
PROJECT_VERSION="2.7.2"

# DMG卷标
-volname "LoimReader"

# 压缩级别 (1-9, 9最高)
-imagekey zlib-level=9
```

#### DMG 背景图设置

1. 编辑 `create_dmg_background.py`：
   ```python
   # 设置背景图尺寸
   width = 600
   height = 400
   
   # 自定义背景颜色、文字等
   ```

2. 重新生成背景图：
   ```bash
   python3 create_dmg_background.py
   ```

### 签名和公证

#### 开发测试签名（本地使用）

```bash
# 自签名（已在脚本中集成）
codesign --force --deep --sign - ctdy123.app
```

#### 正式发布签名

```bash
# 1. 使用开发者证书签名
codesign --force --deep --sign "Developer ID Application: Your Name (TEAM_ID)" ctdy123.app

# 2. 验证签名
codesign --verify --deep --strict --verbose=2 ctdy123.app

# 3. 创建已签名的DMG
hdiutil create -srcfolder dist/ctdy123.app -volname "影谷长图阅读器" \
        -fs HFS+ -format UDZO signed.dmg

# 4. 签名DMG
codesign --sign "Developer ID Application: Your Name (TEAM_ID)" signed.dmg

# 5. 公证（需要App Store Connect API密钥）
xcrun notarytool submit signed.dmg --keychain-profile "notary-profile" --wait

# 6. 装订公证票据
xcrun stapler staple signed.dmg

# 7. 验证公证
xcrun stapler validate signed.dmg
```

### 常见问题

#### 问题1: macdeployqt 未找到

**原因：** Qt 路径未添加到系统 PATH

**解决方案：**
```bash
# 方法1: 添加到环境变量
export PATH="/opt/homebrew/opt/qt@6/bin:$PATH"

# 方法2: 创建符号链接
sudo ln -s /opt/homebrew/opt/qt@6/bin/macdeployqt /usr/local/bin/

# 方法3: 指定完整路径
/opt/homebrew/opt/qt@6/bin/macdeployqt ctdy123.app
```

#### 问题2: 应用无法打开（已损坏）

**原因：** 签名问题或隔离属性

**解决方案：**
```bash
# 移除隔离属性
xattr -cr ctdy123.app

# 重新签名
codesign --force --deep --sign - ctdy123.app

# 如果还不行，允许任何来源（不推荐）
sudo spctl --master-disable
```

#### 问题3: Qt 库依赖错误

**原因：** macdeployqt 未正确复制依赖

**解决方案：**
```bash
# 检查依赖
otool -L build_cmake/bin/ctdy123.app/Contents/MacOS/ctdy123

# 手动运行 macdeployqt
cd build_cmake/bin
macdeployqt ctdy123.app -verbose=2 -always-overwrite

# 检查 Frameworks 目录
ls -la ctdy123.app/Contents/Frameworks/
```

#### 问题4: 图标不显示

**原因：** 图标文件未正确打包

**解决方案：**
```bash
# 1. 重新生成图标
python3 create_app_icon.py

# 2. 检查图标文件
ls -la app_icon.icns

# 3. 确认 Info.plist 设置
plutil -p build_cmake/bin/ctdy123.app/Contents/Info.plist | grep -i icon

# 4. 手动复制图标
cp app_icon.icns build_cmake/bin/ctdy123.app/Contents/Resources/
```

### 测试清单

在发布前，请完成以下测试：

- [ ] DMG 可以正常挂载
- [ ] 应用可以拖拽到 Applications
- [ ] 首次启动无错误提示
- [ ] 所有核心功能正常工作
- [ ] 图标正确显示
- [ ] 关于对话框显示正确版本号
- [ ] 文件关联正常（如果有）
- [ ] 在不同 macOS 版本测试（如果可能）

### 性能测试

```bash
# 1. 启动时间测试
time open -a ctdy123.app

# 2. 内存使用
ps aux | grep ctdy123

# 3. 应用包大小
du -sh build_cmake/bin/ctdy123.app
du -sh dist/影谷长图阅读器_v2.7.dmg

# 4. 依赖库检查
otool -L build_cmake/bin/ctdy123.app/Contents/MacOS/ctdy123 | grep -v "@rpath"
```

### 上传到OSS

```bash
# 方法1: 使用自动生成的上传脚本
cd ../deploy/uploads/loimreader
./upload_to_oss.sh

# 方法2: 构建时直接上传
./onekey_mac_deploy.sh -u

# 方法3: 使用Python脚本
cd ../deploy
python3 upload_to_oss.py loimreader
```

### 验证上传

```bash
# 验证version.json
curl https://limereader-releases.oss-cn-hangzhou.aliyuncs.com/updates/loimreader/version.json

# 验证DMG可访问性
curl -I https://limereader-releases.oss-cn-hangzhou.aliyuncs.com/updates/loimreader/LoimReader_v2.7.2_macOS.dmg
```

---

## Windows 平台

### 快速开始：一键部署

```cmd
onekey_win_deploy.bat
```

脚本会自动完成：
1. 检查Qt和MinGW环境
2. 清理旧的构建文件
3. 配置qmake并编译
4. 部署Qt依赖库
5. 复制MinGW运行时
6. 打包ZIP文件
7. 生成version.json
8. 生成OSS上传脚本

### 输出文件

构建完成后，文件位于：`../deploy/uploads/loimreader/`

```
../deploy/uploads/loimreader/
├── LoimReader_v2.7.2_Windows.zip        # ZIP压缩包
├── version.json                         # 版本信息文件
└── upload_to_oss.bat                    # 上传脚本
```

### 本地测试环境设置

#### 1. Windows Sandbox (推荐 - 免费)
```powershell
# 启用Windows Sandbox (需要Windows 10/11 Pro)
Enable-WindowsOptionalFeature -Online -FeatureName "Containers-DisposableClientVM"
```

**优点：**
- 每次启动都是全新环境
- 启动快速
- 无需额外软件

**缺点：**
- 仅限Windows 10/11 Pro
- 功能相对简单

#### 2. VirtualBox + Windows ISO
1. 下载VirtualBox: https://www.virtualbox.org/
2. 下载Windows ISO: https://www.microsoft.com/software-download/
3. 创建虚拟机：
   - 内存: 2GB+
   - 硬盘: 20GB+
   - 启用网络

#### 3. VMware Player
1. 下载: https://www.vmware.com/products/workstation-player.html
2. 创建虚拟机配置
3. 安装Windows系统

### 测试步骤

#### 1. 构建和打包
```powershell
# 清理并重新打包
.\build.ps1 clean
.\build.ps1 package

# 测试打包完整性
.\test_package.ps1
```

#### 2. 在测试环境中验证
1. 将`dist`文件夹复制到测试环境
2. 运行`dist\run.cmd`
3. 检查是否出现依赖错误

### 常见问题排查

#### 问题 1: GetCurrentPackageFullName错误
**原因：** 缺少MinGW运行时库
**解决：** 确保打包时包含：
- libgcc_s_seh-1.dll
- libstdc++-6.dll
- libwinpthread-1.dll

#### 问题 2: Qt DLL缺失
**原因：** windeployqt未正确部署
**解决：** 使用更完整的windeployqt参数

#### 问题 3: 插件缺失
**原因：** Qt插件目录不完整
**解决：** 检查platforms、imageformats等目录

### 自动化测试脚本

#### 创建测试虚拟机快照
1. 安装干净的Windows系统
2. 创建快照
3. 每次测试前恢复到快照

#### 批量测试脚本
```powershell
# 测试多个Windows版本
$versions = @("Windows 10", "Windows 11", "Windows Server 2019")
foreach ($version in $versions) {
    # 启动对应虚拟机
    # 复制测试文件
    # 运行测试
    # 收集结果
}
```

### 云测试服务

#### Azure Virtual Machines
```bash
# 创建测试VM
az vm create --resource-group test-rg --name test-vm --image Win2019Datacenter --admin-username testuser
```

#### AWS EC2
```bash
# 启动Windows实例
aws ec2 run-instances --image-id ami-windows-2019 --instance-type t2.medium
```

### 测试检查清单

- [ ] 程序能正常启动
- [ ] 所有功能正常工作
- [ ] 没有依赖错误
- [ ] 文件关联正确
- [ ] 卸载程序正常
- [ ] 不同Windows版本兼容性
- [ ] 32位/64位兼容性

### 性能测试

#### 内存使用
```powershell
# 监控内存使用
Get-Process ctdy123 | Select-Object ProcessName, WorkingSet, VirtualMemorySize
```

#### 启动时间
```powershell
# 测量启动时间
Measure-Command { Start-Process "ctdy123.exe" -Wait }
```

### 报告模板

```
测试报告
========
测试环境: Windows 10 Pro (版本号)
测试时间: 2025-01-09
测试人员: [姓名]

测试结果:
- 启动测试: ✓/✗
- 功能测试: ✓/✗  
- 依赖检查: ✓/✗
- 性能测试: ✓/✗

问题记录:
1. [问题描述]
   - 重现步骤:
   - 预期结果:
   - 实际结果:
   - 解决方案:

建议:
- [改进建议]
```
