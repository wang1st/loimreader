# LoimReader 一键部署指南

本指南介绍如何使用统一的部署脚本快速构建、打包和发布 LoimReader 应用。

## 快速开始

### macOS 部署

```bash
# 一键构建和打包（推荐）
./onekey_mac_deploy.sh

# 清理后重新构建
./onekey_mac_deploy.sh -c

# 指定版本号
./onekey_mac_deploy.sh -v 2.7.3

# 构建并自动上传到OSS
./onekey_mac_deploy.sh -u
```

### Windows 部署

```cmd
# 双击运行或在命令行执行
onekey_win_deploy.bat
```

## 版本管理

### 如何发布新版本（例如：2.7.1）

完整的版本更新流程包括源码修改、构建、打包和上传。

#### 步骤 1：修改源代码中的版本号

**需要修改的文件：**

1. **CMakeLists.txt**（主版本号 - 唯一真实来源）
   ```cmake
   # 第4-7行
   project(LoimReader 
       VERSION 2.7.1    # 修改这里：2.7.2 -> 2.7.1
       LANGUAGES CXX
   )
   ```

2. **app_version.h**（回退版本号）
   ```cpp
   // 第42行
   return "2.7.1";  // 修改这里：2.7.2 -> 2.7.1
   ```

3. **onekey_mac_deploy.sh**（部署脚本默认版本）
   ```bash
   # 第17行
   PROJECT_VERSION="2.7.1"  # 修改这里：2.7.2 -> 2.7.1
   ```

**快速修改命令：**

```bash
# 方法1：使用sed批量替换（推荐）
sed -i '' 's/VERSION 2\.7\.2/VERSION 2.7.1/g' CMakeLists.txt
sed -i '' 's/return "2\.7\.2";/return "2.7.1";/g' app_version.h
sed -i '' 's/PROJECT_VERSION="2\.7\.2"/PROJECT_VERSION="2.7.1"/g' onekey_mac_deploy.sh

# 方法2：手动编辑
vim CMakeLists.txt      # 修改 VERSION
vim app_version.h       # 修改回退版本
vim onekey_mac_deploy.sh # 修改默认版本

# 验证修改
grep "VERSION 2\.7\.1" CMakeLists.txt
grep "return \"2\.7\.1\"" app_version.h
grep "PROJECT_VERSION=\"2\.7\.1\"" onekey_mac_deploy.sh
```

#### 步骤 2：提交版本变更到Git

```bash
# 查看修改
git diff CMakeLists.txt app_version.h onekey_mac_deploy.sh

# 提交变更
git add CMakeLists.txt app_version.h onekey_mac_deploy.sh
git commit -m "chore: 版本更新到 2.7.1"

# 打标签
git tag -a v2.7.1 -m "Release version 2.7.1"

# 推送（可选）
git push origin main
git push origin v2.7.1
```

#### 步骤 3：清理并重新构建

```bash
# 清理旧的构建文件
./onekey_mac_deploy.sh -c

# 或使用指定版本号（会覆盖脚本中的默认值）
./onekey_mac_deploy.sh -c -v 2.7.1
```

**构建输出：**
```
../deploy/uploads/loimreader/
├── LoimReader_v2.7.1_macOS.dmg      # 新版本DMG
├── version.json                     # latestVersion: "2.7.1"
└── upload_to_oss.sh                 # 包含版本号的上传脚本
```

#### 步骤 4：测试新版本

```bash
# 1. 检查DMG文件名
ls -lh ../deploy/uploads/loimreader/LoimReader_v2.7.1_macOS.dmg

# 2. 验证version.json内容
cat ../deploy/uploads/loimreader/version.json | python3 -m json.tool

# 应该显示：
# {
#   "latestVersion": "2.7.1",
#   ...
# }

# 3. 挂载DMG测试
open ../deploy/uploads/loimreader/LoimReader_v2.7.1_macOS.dmg

# 4. 安装并运行应用，验证"关于"对话框中的版本号
```

#### 步骤 5：更新发布说明（可选）

编辑生成的 `version.json`，添加详细的更新说明：

```bash
vim ../deploy/uploads/loimreader/version.json
```

修改 `releaseNotes` 字段：
```json
{
  "latestVersion": "2.7.1",
  "releaseDate": "2025-10-20T12:00:00Z",
  "releaseNotes": "LoimReader 版本 2.7.1\n\n新增功能：\n- 添加了XXX功能\n- 改进了YYY性能\n\n修复问题：\n- 修复了ZZZ崩溃问题\n- 修复了AAA显示错误",
  "forceUpdate": false,
  ...
}
```

#### 步骤 6：上传到OSS

```bash
# 进入上传目录
cd ../deploy/uploads/loimreader

# 执行上传脚本
./upload_to_oss.sh
```

**预期输出：**
```
开始上传 LoimReader v2.7.1 到阿里云OSS...
✓ 加载OSS配置: /Users/ethan/Codes/deploy/oss-config.sh
✓ 使用环境变量中的OSS密钥

上传DMG文件...
  上传: LoimReader_v2.7.1_macOS.dmg
  ✓ 上传成功

上传version.json...
✓ version.json上传成功

==========================================
  上传完成！
==========================================

验证链接:
  version.json:
    https://limereader-releases.oss-cn-hangzhou.aliyuncs.com/updates/loimreader/version.json
  DMG:
    https://limereader-releases.oss-cn-hangzhou.aliyuncs.com/updates/loimreader/LoimReader_v2.7.1_macOS.dmg
```

#### 步骤 7：验证发布

```bash
# 1. 验证version.json可访问
curl -s https://limereader-releases.oss-cn-hangzhou.aliyuncs.com/updates/loimreader/version.json | python3 -m json.tool

# 2. 检查DMG可下载
curl -I https://limereader-releases.oss-cn-hangzhou.aliyuncs.com/updates/loimreader/LoimReader_v2.7.1_macOS.dmg

# 3. 测试客户端自动更新（如果有）
# 打开旧版本的应用，检查是否提示更新到2.7.1
```

### 版本更新流程图

```
┌─────────────────────────────────────────────────────────────┐
│                     版本发布完整流程                          │
└─────────────────────────────────────────────────────────────┘

方法一：使用 release.sh（推荐）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  $ ./release.sh 2.7.1
       ↓
  [1] 验证版本号格式
       ↓
  [2] 自动更新源文件（CMakeLists.txt, app_version.h, onekey_mac_deploy.sh）
       ↓
  [3] Git提交和打标签 (v2.7.1)
       ↓
  [4] 清理并构建
       ↓
  [5] 生成 LoimReader_v2.7.1_macOS.dmg 和 version.json
       ↓
  [6] 验证输出
       ↓
  [完成] 提示后续操作


方法二：手动流程
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  [1] 编辑 CMakeLists.txt (VERSION 2.7.1)
       ↓
  [2] 编辑 app_version.h (return "2.7.1")
       ↓
  [3] 编辑 onekey_mac_deploy.sh (PROJECT_VERSION="2.7.1")
       ↓
  [4] Git提交: git commit -m "chore: 版本更新到 2.7.1"
       ↓
  [5] Git标签: git tag -a v2.7.1 -m "Release version 2.7.1"
       ↓
  [6] 构建: ./onekey_mac_deploy.sh -c
       ↓
  [7] 测试本地应用: open build_cmake/bin/LoimReader.app
       ↓
  [8] 测试DMG: open ../deploy/uploads/loimreader/LoimReader_v2.7.1_macOS.dmg
       ↓
  [9] 编辑发布说明: vim ../deploy/uploads/loimreader/version.json
       ↓
  [10] 上传: cd ../deploy/uploads/loimreader && ./upload_to_oss.sh
       ↓
  [11] 验证: curl ...oss.../version.json
       ↓
  [12] 推送: git push origin main && git push origin v2.7.1
       ↓
  [完成]
```

### 实际案例：发布 2.7.1 版本

#### 场景：修复了一个Bug，需要发布 2.7.1

**当前版本：** 2.7.2  
**目标版本：** 2.7.1  
**变更内容：** 修复了页面切换时的崩溃问题

**完整操作流程：**

```bash
# ═══════════════════════════════════════════════════════════
# 步骤 1：使用 release.sh 自动发布
# ═══════════════════════════════════════════════════════════

./release.sh 2.7.1

# 输出示例：
# ========================================
#  版本更新
# ========================================
# 
# 当前版本: 2.7.2
# 新版本:   2.7.1
# 
# 即将执行的操作:
#   1. 更新源代码中的版本号
#   2. 提交到Git并打标签
#   3. 清理并重新构建
#   4. 生成DMG和version.json
# 
# 确认继续? [y/N]: y

# [1/4] 更新源代码中的版本号...
# ✅ 版本号更新成功
#   • CMakeLists.txt: VERSION 2.7.1
#   • app_version.h: return "2.7.1"
#   • onekey_mac_deploy.sh: PROJECT_VERSION="2.7.1"

# [2/4] 提交到Git...
# ✅ Git提交完成
#   • Commit: 版本更新到 2.7.1
#   • Tag: v2.7.1

# [3/4] 构建应用...
# [开始构建过程...]
# ✅ 构建完成

# [4/4] 验证输出文件...
# ✅ DMG文件: ../deploy/uploads/loimreader/LoimReader_v2.7.1_macOS.dmg (48.5M)
# ✅ version.json: 版本号正确 (2.7.1)


# ═══════════════════════════════════════════════════════════
# 步骤 2：编辑发布说明（可选）
# ═══════════════════════════════════════════════════════════

vim ../deploy/uploads/loimreader/version.json

# 修改为：
# {
#   "latestVersion": "2.7.1",
#   "releaseDate": "2025-10-20T12:00:00Z",
#   "releaseNotes": "LoimReader 版本 2.7.1\n\n修复问题：\n- 修复了页面切换时的崩溃问题\n- 优化了内存使用",
#   "forceUpdate": false,
#   ...
# }


# ═══════════════════════════════════════════════════════════
# 步骤 3：测试本地构建的应用程序（上传前验证）
# ═══════════════════════════════════════════════════════════

# 方法1：直接打开构建目录中的.app（推荐）
cd ../../loimreader
open build_cmake/bin/LoimReader.app

# 方法2：使用命令行启动（可看到控制台输出，便于调试）
build_cmake/bin/LoimReader.app/Contents/MacOS/LoimReader

# 测试项：
# ✅ 应用可以正常启动（没有崩溃）
# ✅ 关于对话框显示版本号正确（2.7.1）
# ✅ 所有核心功能正常工作
#    - 打开图片文件
#    - 长图分页显示正常
#    - 导出PDF功能正常
#    - 打印功能正常
# ✅ 登录功能正常
# ✅ 界面显示正常（无乱码、布局错误）

# 注意：
# - 这是在构建目录直接运行，不是从DMG安装
# - 如果有问题，可以快速修改代码重新构建
# - 确认无误后再继续打包和上传


# ═══════════════════════════════════════════════════════════
# 步骤 4：测试DMG安装包（可选）
# ═══════════════════════════════════════════════════════════

cd ../deploy/uploads/loimreader
open LoimReader_v2.7.1_macOS.dmg

# 测试项：
# - DMG可以正常挂载
# - 背景图显示正常
# - 拖拽安装到Applications正常
# - 从Applications启动应用正常
# - 版本号正确

# 注意：如果步骤3已验证通过，此步骤可选


# ═══════════════════════════════════════════════════════════
# 步骤 5：上传到OSS
# ═══════════════════════════════════════════════════════════

cd ../deploy/uploads/loimreader
./upload_to_oss.sh

# 输出：
# 开始上传 LoimReader v2.7.1 到阿里云OSS...
# ✓ 加载OSS配置
# ✓ 使用环境变量中的OSS密钥
# 
# 上传DMG文件...
#   上传: LoimReader_v2.7.1_macOS.dmg
#   ✓ 上传成功
# 
# 上传version.json...
# ✓ version.json上传成功


# ═══════════════════════════════════════════════════════════
# 步骤 6：验证发布
# ═══════════════════════════════════════════════════════════

# 验证version.json
curl -s https://limereader-releases.oss-cn-hangzhou.aliyuncs.com/updates/loimreader/version.json | python3 -m json.tool

# 验证DMG可下载
curl -I https://limereader-releases.oss-cn-hangzhou.aliyuncs.com/updates/loimreader/LoimReader_v2.7.1_macOS.dmg

# 应该返回：HTTP/2 200


# ═══════════════════════════════════════════════════════════
# 步骤 7：推送到Git（如果需要）
# ═══════════════════════════════════════════════════════════

cd ../../loimreader
git push origin main
git push origin v2.7.1


# ═══════════════════════════════════════════════════════════
# 完成！🎉
# ═══════════════════════════════════════════════════════════
```

### 版本号命名规范

LoimReader 采用 [语义化版本](https://semver.org/lang/zh-CN/) 规范：`主版本号.次版本号.修订号`

```
版本格式：MAJOR.MINOR.PATCH (例如：2.7.1)

- MAJOR (主版本号): 重大更新，可能不兼容旧版本
  例如：1.x.x -> 2.x.x

- MINOR (次版本号): 新增功能，向后兼容
  例如：2.6.x -> 2.7.x

- PATCH (修订号): Bug修复，向后兼容
  例如：2.7.0 -> 2.7.1
```

**版本更新示例：**

| 变更类型 | 旧版本 | 新版本 | 说明 |
|---------|--------|--------|------|
| 重大更新 | 2.7.1 | 3.0.0 | 架构重构，不兼容旧版 |
| 新增功能 | 2.7.1 | 2.8.0 | 添加新功能 |
| Bug修复 | 2.7.1 | 2.7.2 | 修复已知问题 |
| 紧急修复 | 2.7.1 | 2.7.1.1 | 热修复（特殊情况） |

### 快速版本发布脚本（推荐）

项目已提供 `release.sh` 脚本，可以一键完成版本更新的所有步骤。

**基本用法：**

```bash
# 发布新版本（交互式确认）
./release.sh 2.7.1

# 发布并自动上传到OSS
./release.sh 2.7.1 -u

# 跳过Git操作（仅更新和构建）
./release.sh 2.7.1 -s

# 查看帮助
./release.sh -h
```

**脚本会自动完成：**

1. ✅ 验证版本号格式
2. ✅ 显示当前版本和新版本
3. ✅ 更新所有源文件中的版本号
4. ✅ 提交到Git并创建标签
5. ✅ 清理并重新构建
6. ✅ 生成DMG和version.json
7. ✅ 验证输出文件
8. ✅ 可选：自动上传到OSS

**完整示例：**

```bash
# 场景1：发布Bug修复版本（2.7.2 -> 2.7.3）
./release.sh 2.7.3

# 场景2：发布新功能版本（2.7.3 -> 2.8.0）
./release.sh 2.8.0 -u  # 构建并上传

# 场景3：仅本地测试，不提交Git
./release.sh 2.7.1-beta -s

# 场景4：快速迭代（跳过Git，直接构建）
./release.sh 2.7.1 -s
```

### 版本发布检查清单

使用 `release.sh` 发布新版本时，请确认：

**发布前：**
- [ ] 所有功能已测试通过
- [ ] 代码已合并到主分支
- [ ] 更新日志已准备好
- [ ] 确定新版本号

**发布中：**
- [ ] `release.sh` 执行成功
- [ ] 版本号在所有文件中一致
- [ ] DMG文件名正确
- [ ] version.json格式正确

**发布后：**
- [ ] DMG可以正常安装
- [ ] 应用显示正确版本号
- [ ] 所有功能正常工作
- [ ] OSS链接可访问
- [ ] Git已推送（如果需要）

## 环境准备

### macOS 环境

```bash
# 1. 安装必需工具
brew install qt@6 cmake python3

# 2. 安装Python依赖
pip3 install Pillow

# 3. 添加Qt到PATH（如果需要）
export PATH="/opt/homebrew/opt/qt@6/bin:$PATH"

# 4. 安装ossutil（用于上传到OSS）
./install_ossutil.sh

# 或者手动安装（Apple Silicon）
curl -o ossutil.zip https://gosspublic.alicdn.com/ossutil/v2/2.2.0/ossutil-2.2.0-mac-arm64.zip
unzip ossutil.zip
cd ossutil-2.2.0-mac-arm64
chmod 755 ossutil
sudo mv ossutil /usr/local/bin/

# 验证
ossutil version
```

### Windows 环境

1. **安装Qt 6.8.1 MinGW**
   - 下载：https://www.qt.io/download
   - 设置环境变量：`QT_DIR=D:\Qt\6.8.1\mingw_64`

2. **设置MinGW路径**
   - 通常位于：`D:\Qt\Tools\mingw1120_64`
   - 或设置环境变量：`MINGW_DIR=D:\Qt\Tools\mingw1120_64`

3. **安装ossutil（可选，用于上传）**
   - 下载：https://help.aliyun.com/zh/oss/developer-tools/ossutil
   - 配置：`ossutil64 config`

## 输出文件说明

### macOS 输出

构建完成后，文件位于：`../deploy/uploads/loimreader/`

```
../deploy/uploads/loimreader/
├── LoimReader_v2.7.2_macOS.dmg          # DMG安装包
├── version.json                         # 版本信息文件
└── upload_to_oss.sh                     # OSS上传脚本
```

### Windows 输出

构建完成后，文件位于：`../deploy/uploads/loimreader/`

```
../deploy/uploads/loimreader/
├── LoimReader_v2.7.2_Windows.zip        # ZIP压缩包
├── version.json                         # 版本信息文件
└── upload_to_oss.bat                    # OSS上传脚本
```

### version.json 格式

```json
{
  "latestVersion": "2.7.2",
  "releaseDate": "2025-10-20T10:30:00Z",
  "releaseNotes": "LoimReader 版本 2.7.2\n- 性能优化\n- Bug修复\n- 功能改进",
  "forceUpdate": false,
  "packages": {
    "macos": {
      "url": "LoimReader_v2.7.2_macOS.dmg",
      "size": "48.5M",
      "hash": "abc123def456...",
      "downloadUrl": "https://limereader-releases.oss-cn-hangzhou.aliyuncs.com/updates/loimreader/LoimReader_v2.7.2_macOS.dmg",
      "minSystemVersion": "macOS 14.0 或更高版本"
    },
    "windows": {
      "url": "LoimReader_v2.7.2_Windows.zip",
      "size": "46 MB",
      "hash": "def456abc123...",
      "downloadUrl": "https://limereader-releases.oss-cn-hangzhou.aliyuncs.com/updates/loimreader/LoimReader_v2.7.2_Windows.zip",
      "minSystemVersion": "Windows 10 或更高版本"
    }
  }
}
```

## 上传到阿里云OSS

### 自动上传（构建时）

```bash
# macOS - 构建并上传
./onekey_mac_deploy.sh -u

# Windows - 需要手动运行上传脚本
onekey_win_deploy.bat
# 然后运行生成的上传脚本
..\deploy\uploads\loimreader\upload_to_oss.bat
```

### 手动上传（构建后）

```bash
# macOS
cd ../deploy/uploads/loimreader
./upload_to_oss.sh

# Windows
cd ..\deploy\uploads\loimreader
upload_to_oss.bat
```

### 验证上传

```bash
# 验证version.json
curl https://limereader-releases.oss-cn-hangzhou.aliyuncs.com/updates/loimreader/version.json

# 验证DMG/ZIP（检查HTTP状态）
curl -I https://limereader-releases.oss-cn-hangzhou.aliyuncs.com/updates/loimreader/LoimReader_v2.7.2_macOS.dmg
curl -I https://limereader-releases.oss-cn-hangzhou.aliyuncs.com/updates/loimreader/LoimReader_v2.7.2_Windows.zip
```

## 脚本选项说明

### onekey_mac_deploy.sh 选项

| 选项 | 说明 | 示例 |
|------|------|------|
| `-v, --version VERSION` | 指定版本号 | `-v 2.7.3` |
| `-c, --clean` | 清理后构建 | `-c` |
| `-s, --skip-build` | 跳过构建，仅打包 | `-s` |
| `-u, --upload` | 构建后自动上传OSS | `-u` |
| `-h, --help` | 显示帮助信息 | `-h` |

### 使用示例

```bash
# 场景1: 日常开发测试
./onekey_mac_deploy.sh

# 场景2: 准备发布新版本
./onekey_mac_deploy.sh -c -v 2.7.3

# 场景3: 快速发布（清理+构建+上传）
./onekey_mac_deploy.sh -c -u

# 场景4: 仅重新打包（不重新编译）
./onekey_mac_deploy.sh -s

# 场景5: 查看帮助
./onekey_mac_deploy.sh -h
```

## 常见问题

### macOS

#### Q: 找不到 macdeployqt

**解决方法：**
```bash
export PATH="/opt/homebrew/opt/qt@6/bin:$PATH"
# 或
brew link qt@6
```

#### Q: 应用无法打开（已损坏）

**解决方法：**
```bash
# 移除隔离属性
xattr -cr ../deploy/uploads/loimreader/LoimReader.app

# 或在系统设置中允许
# 系统设置 -> 隐私与安全性 -> 安全性 -> 仍要打开
```

#### Q: DMG创建失败

**检查：**
1. 磁盘空间是否充足
2. 临时目录权限是否正确
3. hdiutil 是否可用

### Windows

#### Q: Qt 或 MinGW 未找到

**解决方法：**
```cmd
# 设置环境变量
set QT_DIR=D:\Qt\6.8.1\mingw_64
set MINGW_DIR=D:\Qt\Tools\mingw1120_64

# 或在系统环境变量中永久设置
```

#### Q: 编译失败

**检查：**
1. Qt 版本是否为 6.8.1
2. MinGW 版本是否匹配
3. clps.pro 文件是否存在

#### Q: ZIP 创建失败

**解决方法：**
```cmd
# 确保PowerShell可用
powershell -Command "Get-Host"

# 手动打包
cd dist\bin
powershell -Command "Compress-Archive -Path * -DestinationPath ..\..\LoimReader.zip"
```

## 完整工作流程

### 开发流程

```bash
# 1. 修改代码
vim mainwindow.cpp

# 2. 快速构建测试
./onekey_mac_deploy.sh

# 3. 测试应用
open ../deploy/uploads/loimreader/LoimReader_v2.7.2_macOS.dmg

# 4. 确认无误后上传
cd ../deploy/uploads/loimreader
./upload_to_oss.sh
```

### 发布流程

```bash
# 1. 更新版本号
# 编辑 CMakeLists.txt: VERSION 2.7.3
# 或使用参数: -v 2.7.3

# 2. 清理构建
./onekey_mac_deploy.sh -c -v 2.7.3

# 3. 全面测试
open ../deploy/uploads/loimreader/LoimReader_v2.7.3_macOS.dmg
# 测试所有功能

# 4. 更新发布说明
# 编辑 version.json 中的 releaseNotes

# 5. 上传到OSS
cd ../deploy/uploads/loimreader
./upload_to_oss.sh

# 6. 验证
curl https://limereader-releases.oss-cn-hangzhou.aliyuncs.com/updates/loimreader/version.json
```

## OSS 目录结构

```
limereader-releases (Bucket)
└── updates/
    └── loimreader/
        ├── version.json
        ├── LoimReader_v2.7.2_macOS.dmg
        ├── LoimReader_v2.7.2_Windows.zip
        ├── LoimReader_v2.7.3_macOS.dmg      # 新版本
        └── LoimReader_v2.7.3_Windows.zip    # 新版本
```

## 性能优化建议

### 加速编译

```bash
# macOS - 使用更多CPU核心（已在脚本中自动使用）
make -j$(sysctl -n hw.ncpu)

# Windows - 增加并行编译
mingw32-make -j8
```

### 减小包体积

1. **移除不必要的Qt插件**
   - 脚本已自动优化
   
2. **使用最高压缩级别**
   - DMG: `-imagekey zlib-level=9`（已启用）
   - ZIP: PowerShell默认压缩

### 缓存构建

```bash
# 保留构建缓存，避免每次清理
./onekey_mac_deploy.sh  # 不使用 -c 参数
```

## 团队协作

### 新成员入职

```bash
# 1. 克隆仓库
git clone <repository-url>
cd loimreader

# 2. 安装依赖
# macOS
brew install qt@6 cmake python3
pip3 install Pillow

# Windows: 安装 Qt 6.8.1 MinGW

# 3. 首次构建
./onekey_mac_deploy.sh -c

# 4. 测试
open ../deploy/uploads/loimreader/LoimReader_v2.7.2_macOS.dmg
```

### 版本管理规范

- **主版本号**：重大更新（2.x.x -> 3.x.x）
- **次版本号**：新功能（2.7.x -> 2.8.x）
- **修订号**：Bug修复（2.7.2 -> 2.7.3）

### Git 工作流

```bash
# 1. 创建功能分支
git checkout -b feature/new-feature

# 2. 开发和测试
./onekey_mac_deploy.sh

# 3. 提交代码
git add .
git commit -m "feat: 添加新功能"

# 4. 合并到主分支
git checkout main
git merge feature/new-feature

# 5. 发布版本
./onekey_mac_deploy.sh -c -v 2.7.3 -u
git tag v2.7.3
git push origin v2.7.3
```

## 相关文档

- **OSS发布指南：** `../deploy/OSS_RELEASE_GUIDE.md`
- **构建打包指南：** `构建打包指南.md`
- **项目README：** `README.md`

## 技术支持

遇到问题？

1. 查看本文档的"常见问题"章节
2. 检查脚本输出的错误信息
3. 查看 `../deploy/OSS_RELEASE_GUIDE.md`
4. 联系技术团队

---

**版本：** 1.0.0  
**更新日期：** 2025-10-20  
**维护者：** LoimReader Team

