# 开始使用 LoimReader 部署系统

欢迎！这是最简单的入门指南。

## 🎯 你想做什么？

### 我想发布新版本 2.7.1

**最简单的方式：**

```bash
./release.sh 2.7.1 -u
```

这一条命令会：
1. ✅ 自动更新所有文件中的版本号
2. ✅ 提交到Git并打标签
3. ✅ 清理并构建应用
4. ✅ 生成DMG和version.json
5. ✅ 上传到阿里云OSS
6. ✅ 提示你推送到Git

**完成！** 🎉

**详细说明：** [一键部署指南.md - 版本管理](./一键部署指南.md#版本管理)

---

### 我只想构建DMG测试

```bash
./onekey_mac_deploy.sh
```

DMG会生成在：`../deploy/uploads/loimreader/LoimReader_v2.7.2_macOS.dmg`

**打开测试：**
```bash
open ../deploy/uploads/loimreader/LoimReader_v2.7.2_macOS.dmg
```

---

### 我需要安装 ossutil

```bash
./install_ossutil.sh
```

**验证：**
```bash
ossutil version
# 输出：2.2.0
```

---

### 我想检查环境是否正确

```bash
./verify_setup.sh
```

会自动检查：
- ✅ Qt
- ✅ CMake
- ✅ Python
- ✅ ossutil
- ✅ OSS密钥配置
- ✅ 文档完整性

---

### 我想上传已构建的文件

```bash
cd ../deploy/uploads/loimreader
./upload_to_oss.sh
```

---

## 📋 完整发布流程（新手版）

### 步骤1：准备环境（首次使用）

```bash
# 安装工具
brew install qt@6 cmake python3
pip3 install Pillow

# 安装ossutil
./install_ossutil.sh

# 验证环境
./verify_setup.sh
```

### 步骤2：发布新版本

```bash
# 一条命令完成
./release.sh 2.7.1 -u

# 按提示确认后，等待完成
```

### 步骤3：验证发布

```bash
# 验证OSS链接
curl -s https://limereader-releases.oss-cn-hangzhou.aliyuncs.com/updates/loimreader/version.json | python3 -m json.tool
```

### 步骤4：推送Git

```bash
git push origin main
git push origin v2.7.1
```

**完成！** 总耗时约 15-20 分钟。

---

## 🆘 遇到问题？

### ossutil 找不到

```bash
./install_ossutil.sh
ossutil version
```

### Qt 找不到

```bash
export PATH="/opt/homebrew/opt/qt@6/bin:$PATH"
```

### 构建失败

```bash
./verify_setup.sh  # 检查环境
./onekey_mac_deploy.sh -c  # 清理后重试
```

### 版本号不一致

```bash
./release.sh 2.7.1  # 自动同步所有版本号
```

---

## 📖 下一步

### 想了解更多？

- **完整部署流程：** [一键部署指南.md](./一键部署指南.md)
- **快速命令速查：** [快速参考.md](./快速参考.md)
- **所有文档列表：** [文档索引.md](./文档索引.md)

### 打印这个速查卡

```
┌─────────────────────────────────────────┐
│   LoimReader 部署系统速查卡              │
├─────────────────────────────────────────┤
│                                         │
│ 发布新版本：                             │
│   ./release.sh 2.7.1 -u                │
│                                         │
│ 快速构建：                               │
│   ./onekey_mac_deploy.sh               │
│                                         │
│ 验证环境：                               │
│   ./verify_setup.sh                    │
│                                         │
│ 安装ossutil：                           │
│   ./install_ossutil.sh                 │
│                                         │
│ 输出目录：                               │
│   ../deploy/uploads/loimreader/        │
│                                         │
│ 帮助文档：                               │
│   README.md（项目总览）                  │
│   一键部署指南.md（详细文档）             │
│   快速参考.md（一页速查）                 │
│                                         │
└─────────────────────────────────────────┘
```

---

**还有问题？** 查看 [文档索引.md](./文档索引.md) 找到对应的详细文档。

**立即开始：**

```bash
# 验证环境
./verify_setup.sh

# 如果通过，就可以开始了！
./release.sh 2.7.1
```

祝你使用愉快！🚀

