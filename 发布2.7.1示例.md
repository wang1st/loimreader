# 发布 2.7.1 版本完整示例

这是一个真实的发布流程示例，展示如何从 2.7.2 发布到 2.7.1。

## 前提条件

- 已完成所有代码修改和测试
- 确认要发布的新版本号：2.7.1
- 已安装所有必要工具（Qt, CMake, ossutil等）

## 完整操作记录

### 终端操作过程

```bash
# ═══════════════════════════════════════════════════════════
# 准备工作
# ═══════════════════════════════════════════════════════════

# 1. 确保在正确的目录
cd /Users/ethan/Codes/loimreader

# 2. 确保主分支是最新的
git checkout main
git pull origin main

# 3. 查看当前版本
grep "VERSION" CMakeLists.txt
# 输出: VERSION 2.7.2


# ═══════════════════════════════════════════════════════════
# 方式一：使用 release.sh（推荐，最简单）
# ═══════════════════════════════════════════════════════════

./release.sh 2.7.1

# 屏幕输出：
# ========================================
#  版本更新
# ========================================
# 
# 当前版本: 2.7.2
# 新版本:   2.7.1
# 
# 即将执行的操作:
#   1. 更新源代码中的版本号
#   2. 提交到Git并打标签
#   3. 清理并重新构建
#   4. 生成DMG和version.json
# 
# 确认继续? [y/N]: y ← 输入 y

# [1/4] 更新源代码中的版本号...
# ✅ 版本号更新成功
#   • CMakeLists.txt: VERSION 2.7.1
#   • app_version.h: return "2.7.1"
#   • onekey_mac_deploy.sh: PROJECT_VERSION="2.7.1"
# 
# [2/4] 提交到Git...
# ✅ Git提交完成
#   • Commit: 版本更新到 2.7.1
#   • Tag: v2.7.1
# 
# [3/4] 构建应用...
# ========================================
#  LoimReader macOS 一键部署脚本 v2.7.1
# ========================================
# 
# [INFO] 检查构建环境...
# [SUCCESS] 环境检查通过
# [INFO] 清理构建文件...
# [SUCCESS] 清理完成
# [INFO] 开始构建应用...
# [... 构建过程 ...]
# [SUCCESS] 应用构建完成
# [INFO] 创建DMG安装包...
# [SUCCESS] DMG创建成功
# [INFO] 生成version.json...
# [SUCCESS] version.json生成成功
# 
# [4/4] 验证输出文件...
# ✅ DMG文件: ../deploy/uploads/loimreader/LoimReader_v2.7.1_macOS.dmg (48.5M)
# ✅ version.json: 版本号正确 (2.7.1)
# 
# ========================================
#  发布完成
# ========================================
# 
# 版本: 2.7.1
# 
# 后续步骤:
# 
# 1. 测试应用:
#    open ../deploy/uploads/loimreader/LoimReader_v2.7.1_macOS.dmg
# 
# 2. 编辑发布说明（可选）:
#    vim ../deploy/uploads/loimreader/version.json
# 
# 3. 上传到OSS:
#    cd ../deploy/uploads/loimreader
#    ./upload_to_oss.sh
# 
# 4. 推送到Git:
#    git push origin main
#    git push origin v2.7.1


# ═══════════════════════════════════════════════════════════
# 测试应用
# ═══════════════════════════════════════════════════════════

# 打开DMG
open ../deploy/uploads/loimreader/LoimReader_v2.7.1_macOS.dmg

# 在Finder中：
# 1. 拖拽 LoimReader.app 到 Applications
# 2. 在Applications中打开应用
# 3. 菜单 -> 关于LoimReader -> 确认显示 "版本 2.7.1"
# 4. 测试所有主要功能
# 5. 确认崩溃问题已修复


# ═══════════════════════════════════════════════════════════
# 编辑发布说明
# ═══════════════════════════════════════════════════════════

vim ../deploy/uploads/loimreader/version.json

# 修改 releaseNotes 字段：
# {
#   "latestVersion": "2.7.1",
#   "releaseDate": "2025-10-20T14:30:00Z",
#   "releaseNotes": "LoimReader 版本 2.7.1\n\n本次更新：\n\n🐛 Bug修复\n- 修复了页面切换时偶发的崩溃问题\n- 修复了大尺寸图片加载时的内存溢出\n- 修复了打印预览时的布局错误\n\n⚡️ 性能优化\n- 优化了图片渲染性能，提升30%\n- 减少了内存占用\n\n建议所有用户升级。",
#   "forceUpdate": false,
#   ...
# }

# 保存后验证JSON格式
python3 -m json.tool ../deploy/uploads/loimreader/version.json > /dev/null
echo "✅ JSON格式正确"


# ═══════════════════════════════════════════════════════════
# 上传到OSS
# ═══════════════════════════════════════════════════════════

cd ../deploy/uploads/loimreader
./upload_to_oss.sh

# 输出：
# 开始上传 LoimReader v2.7.1 到阿里云OSS...
# ✓ 加载OSS配置: /Users/ethan/Codes/deploy/oss-config.sh
# ✓ 使用环境变量中的OSS密钥
# 
# 上传DMG文件...
#   上传: LoimReader_v2.7.1_macOS.dmg
#   ✓ 上传成功
# 
# 上传version.json...
# ✓ version.json上传成功
# 
# ==========================================
#   上传完成！
# ==========================================
# 
# 验证链接:
#   version.json:
#     https://limereader-releases.oss-cn-hangzhou.aliyuncs.com/updates/loimreader/version.json
#   DMG:
#     https://limereader-releases.oss-cn-hangzhou.aliyuncs.com/updates/loimreader/LoimReader_v2.7.1_macOS.dmg


# ═══════════════════════════════════════════════════════════
# 验证发布
# ═══════════════════════════════════════════════════════════

# 验证version.json内容
curl -s https://limereader-releases.oss-cn-hangzhou.aliyuncs.com/updates/loimreader/version.json | python3 -m json.tool

# 输出：
# {
#   "latestVersion": "2.7.1",
#   "releaseDate": "2025-10-20T14:30:00Z",
#   "releaseNotes": "LoimReader 版本 2.7.1...",
#   ...
# }

# 验证DMG可访问
curl -I https://limereader-releases.oss-cn-hangzhou.aliyuncs.com/updates/loimreader/LoimReader_v2.7.1_macOS.dmg

# 输出：
# HTTP/2 200
# content-type: application/octet-stream
# content-length: 50876543
# ...

# 测试下载（可选）
curl -o /tmp/test_download.dmg https://limereader-releases.oss-cn-hangzhou.aliyuncs.com/updates/loimreader/LoimReader_v2.7.1_macOS.dmg
open /tmp/test_download.dmg


# ═══════════════════════════════════════════════════════════
# 推送到Git
# ═══════════════════════════════════════════════════════════

cd ../../loimreader

# 查看Git状态
git status
# 输出: On branch main
#       nothing to commit, working tree clean

# 查看最新提交
git log -1 --oneline
# 输出: abc1234 chore: 版本更新到 2.7.1

# 查看标签
git tag | grep 2.7.1
# 输出: v2.7.1

# 推送
git push origin main
git push origin v2.7.1

# 输出：
# To github.com:yourname/loimreader.git
#    abc1234..def5678  main -> main
#  * [new tag]         v2.7.1 -> v2.7.1


# ═══════════════════════════════════════════════════════════
# 完成！
# ═══════════════════════════════════════════════════════════

echo "🎉 版本 2.7.1 发布完成！"
```

## 时间线预估

| 步骤 | 操作 | 预计耗时 |
|------|------|---------|
| 1 | 运行 release.sh | 1分钟 |
| 2 | 构建应用 | 5-10分钟 |
| 3 | 测试DMG | 3-5分钟 |
| 4 | 编辑发布说明 | 2-3分钟 |
| 5 | 上传到OSS | 2-5分钟 |
| 6 | 验证和推送Git | 1分钟 |
| **总计** | | **15-25分钟** |

## 常见场景

### 场景1：紧急Bug修复

```bash
# 修复了紧急问题，快速发布
./release.sh 2.7.1 -u  # 构建并上传

# 立即推送Git
git push origin main
git push origin v2.7.1

# 总耗时：约15分钟
```

### 场景2：正常功能更新

```bash
# 添加了新功能，正常流程发布
./release.sh 2.8.0

# 详细测试
open ../deploy/uploads/loimreader/LoimReader_v2.8.0_macOS.dmg

# 编辑详细的发布说明
vim ../deploy/uploads/loimreader/version.json

# 确认无误后上传
cd ../deploy/uploads/loimreader
./upload_to_oss.sh

# 推送Git
cd ../../loimreader
git push origin main
git push origin v2.8.0

# 总耗时：约25分钟
```

### 场景3：测试版本（不上传）

```bash
# 内部测试版本，不提交Git
./release.sh 2.7.1-beta -s

# 仅本地测试
open ../deploy/uploads/loimreader/LoimReader_v2.7.1-beta_macOS.dmg

# 不上传到OSS
# 不推送Git

# 总耗时：约10分钟
```

### 场景4：修改后重新发布

```bash
# 如果发现问题需要重新发布同一版本

# 1. 修改代码
vim mainwindow.cpp

# 2. 重新构建（不修改版本号）
./onekey_mac_deploy.sh -c

# 3. 测试
open ../deploy/uploads/loimreader/LoimReader_v2.7.1_macOS.dmg

# 4. 确认后重新上传（覆盖）
cd ../deploy/uploads/loimreader
./upload_to_oss.sh

# 总耗时：约10分钟
```

## 版本号决策树

```
需要发布新版本？
    ↓
    是
    ↓
什么类型的更新？
    ↓
┌───┴───┬────────────┬──────────┐
│       │            │          │
重大改动  新增功能     Bug修复    文档/样式
↓       ↓            ↓          ↓
3.0.0   2.8.0        2.7.3      2.7.2（不改版本）
│       │            │
主版本+1 次版本+1     修订号+1
次版本=0 修订号=0
修订号=0
```

## 版本更新记录模板

建议在项目中维护 `CHANGELOG.md`：

```markdown
# 更新日志

## [2.7.1] - 2025-10-20

### 修复
- 修复了页面切换时的崩溃问题 (#123)
- 修复了大尺寸图片加载时的内存溢出 (#124)
- 修复了打印预览时的布局错误 (#125)

### 优化
- 优化了图片渲染性能，提升30%
- 减少了内存占用

### 技术
- 更新Qt依赖库版本
- 改进了错误处理机制

## [2.7.2] - 2025-10-15

### 新增
- 添加了自动切边功能
- 支持拖拽多个文件

### 修复
- 修复了启动时的闪退问题

...
```

## 回滚操作

如果发布后发现重大问题，需要回滚：

```bash
# 1. 回滚代码
git revert HEAD
git commit -m "revert: 回滚版本2.7.1"

# 2. 删除标签
git tag -d v2.7.1
git push origin :refs/tags/v2.7.1

# 3. 重新发布修复版本
./release.sh 2.7.2
```

## 检查清单

### 发布前检查

- [ ] 所有代码已提交
- [ ] 所有测试通过
- [ ] 更新日志已准备
- [ ] 版本号格式正确
- [ ] 在主分支上操作

### 发布后检查

- [ ] DMG文件名包含正确版本号
- [ ] version.json 中 latestVersion 正确
- [ ] Git标签已创建
- [ ] 应用"关于"对话框显示正确版本
- [ ] OSS文件可访问
- [ ] Git已推送到远程

## 常见问题

### Q: 我改错版本号了怎么办？

```bash
# 如果还没推送Git
git reset --soft HEAD~1  # 撤销提交
git tag -d v2.7.1        # 删除标签

# 重新运行正确的版本号
./release.sh 2.7.2
```

### Q: 构建失败了怎么办？

```bash
# release.sh 会自动处理失败，不会提交Git
# 修复问题后重新运行即可
./release.sh 2.7.1
```

### Q: 只想更新版本号，不构建？

```bash
# 使用sed手动更新
sed -i '' 's/VERSION 2\.7\.2/VERSION 2.7.1/g' CMakeLists.txt
sed -i '' 's/return "2\.7\.2";/return "2.7.1";/g' app_version.h
sed -i '' 's/PROJECT_VERSION="2\.7\.2"/PROJECT_VERSION="2.7.1"/g' onekey_mac_deploy.sh

# 提交
git add CMakeLists.txt app_version.h onekey_mac_deploy.sh
git commit -m "chore: 版本更新到 2.7.1"
```

### Q: 如何发布多平台版本？

```bash
# macOS
./release.sh 2.7.1

# Windows（在Windows机器上）
onekey_win_deploy.bat
# 手动修改版本号或使用类似的脚本

# 两个平台的文件会在同一个目录：
# ../deploy/uploads/loimreader/
#   ├── LoimReader_v2.7.1_macOS.dmg
#   └── LoimReader_v2.7.1_Windows.zip

# 合并version.json后上传
```

## 自动化建议

### 使用Git Hooks

创建 `.git/hooks/pre-commit`：

```bash
#!/bin/bash
# 检查版本号一致性

CMAKE_VERSION=$(grep "VERSION" CMakeLists.txt | grep -o '[0-9]\+\.[0-9]\+\.[0-9]\+')
HEADER_VERSION=$(grep "return" app_version.h | grep -o '[0-9]\+\.[0-9]\+\.[0-9]\+')
SCRIPT_VERSION=$(grep "PROJECT_VERSION" onekey_mac_deploy.sh | grep -o '[0-9]\+\.[0-9]\+\.[0-9]\+')

if [ "$CMAKE_VERSION" != "$HEADER_VERSION" ] || [ "$CMAKE_VERSION" != "$SCRIPT_VERSION" ]; then
    echo "错误: 版本号不一致"
    echo "  CMakeLists.txt: $CMAKE_VERSION"
    echo "  app_version.h: $HEADER_VERSION"
    echo "  onekey_mac_deploy.sh: $SCRIPT_VERSION"
    exit 1
fi
```

### GitHub Actions自动发布

```yaml
# .github/workflows/release.yml
name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build-macos:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup Environment
        run: |
          brew install qt@6 cmake python3
          pip3 install Pillow
      
      - name: Build
        run: ./onekey_mac_deploy.sh -c
      
      - name: Upload to OSS
        env:
          OSS_ACCESS_KEY_ID: ${{ secrets.OSS_ACCESS_KEY_ID }}
          OSS_ACCESS_KEY_SECRET: ${{ secrets.OSS_ACCESS_KEY_SECRET }}
        run: |
          cd ../deploy/uploads/loimreader
          ./upload_to_oss.sh
      
      - name: Create Release
        uses: actions/create-release@v1
        with:
          tag_name: ${{ github.ref }}
          release_name: Release ${{ github.ref }}
```

---

**文档日期：** 2025-10-20  
**示例版本：** 2.7.1  
**预计总耗时：** 15-25分钟

